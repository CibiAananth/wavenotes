#!/bin/bash

# Step 1: SCP "web" and "app" to GCP "~" location

# Step 2: Delete existing folders and rename "web" to "backend" and "app" to "frontend"

# Step 3: Kill running "socket" session, create a new one, go to "backend", and install dependencies

# Step 4: Detach from the tmux session
